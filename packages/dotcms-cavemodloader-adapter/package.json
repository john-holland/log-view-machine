{
  "name": "dotcms-cavemodloader-adapter",
  "version": "1.0.0",
  "description": "Mod loading config for dotCMS: timeout, CORS, base URL; tenant-aware getModLoaderConfig",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
